<UserControl xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:l="using:LoomX.Localization" x:Class="LoomX.Views.ActivityView">
  <UserControl.Resources>
    <DataTemplate x:Key="ActivityFilterTemplate"><TextBlock Text="{Binding DisplayName}"/></DataTemplate>
  </UserControl.Resources>
  <UserControl.Styles>
    <Style Selector="Border.panel"><Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CornerRadius" Value="10"/></Style>
    <Style Selector="ListBox.activity-list"><Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="0"/></Style>
    <Style Selector="ListBox.activity-list ListBoxItem"><Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="0"/><Setter Property="HorizontalContentAlignment" Value="Stretch"/></Style>
    <Style Selector="ListBox.activity-list ListBoxItem:selected"><Setter Property="Background" Value="Transparent"/></Style>
    <Style Selector="Button.outline"><Setter Property="MinHeight" Value="34"/><Setter Property="Padding" Value="11,7"/><Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/></Style>
    <Style Selector="Button.row"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="0"/></Style>
    <Style Selector="Button.row:pointerover"><Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/></Style>
    <Style Selector="ComboBox.filter"><Setter Property="MinWidth" Value="128"/><Setter Property="MinHeight" Value="36"/><Setter Property="Background" Value="{DynamicResource SurfaceSubtleBrush}"/></Style>
    <Style Selector="TextBox.filter"><Setter Property="MinHeight" Value="36"/><Setter Property="Background" Value="{DynamicResource SurfaceSubtleBrush}"/><Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/></Style>
  </UserControl.Styles>
  <Grid RowDefinitions="Auto,*" RowSpacing="14">
    <Grid Grid.Row="0" ColumnDefinitions="*,*,*,*" ColumnSpacing="10">
      <Border Classes="panel" Padding="14"><StackPanel Spacing="4"><TextBlock Text="{l:Locale activity.stat.requestcount.label}" Opacity="0.62" FontSize="11"/><TextBlock Text="{Binding TotalCount}" FontSize="22" FontWeight="Bold"/><TextBlock Text="{l:Locale activity.stat.requestcount.sub}" Opacity="0.52" FontSize="10"/></StackPanel></Border>
      <Border Grid.Column="1" Classes="panel" Padding="14"><StackPanel Spacing="4"><TextBlock Text="{l:Locale activity.stat.conversion.label}" Opacity="0.62" FontSize="11"/><TextBlock Text="{Binding ConversionCount}" FontSize="22" FontWeight="Bold"/><TextBlock Text="{l:Locale activity.stat.conversion.sub}" Opacity="0.52" FontSize="10"/></StackPanel></Border>
      <Border Grid.Column="2" Classes="panel" Padding="14"><StackPanel Spacing="4"><TextBlock Text="{l:Locale activity.stat.failures.label}" Opacity="0.62" FontSize="11"/><TextBlock Text="{Binding FailureCount}" FontSize="22" FontWeight="Bold"/><TextBlock Text="{l:Locale activity.stat.failures.sub}" Opacity="0.52" FontSize="10"/></StackPanel></Border>
      <Border Grid.Column="3" Classes="panel" Padding="14"><StackPanel Spacing="4"><TextBlock Text="{l:Locale activity.stat.p95.label}" Opacity="0.62" FontSize="11"/><TextBlock Text="{Binding P95Latency}" FontSize="22" FontWeight="Bold"/><TextBlock Text="{l:Locale activity.stat.p95.sub}" Opacity="0.52" FontSize="10"/></StackPanel></Border>
    </Grid>
    <Grid Grid.Row="1" ColumnDefinitions="1.45*,0.75*" ColumnSpacing="14" MinHeight="420">
      <Border Grid.Column="0" Classes="panel">
        <Grid RowDefinitions="Auto,*">
          <Grid Row="0" ColumnDefinitions="*,Auto,Auto,Auto" ColumnSpacing="8" Margin="10">
            <TextBox Classes="filter" Watermark="{l:Locale activity.search.placeholder}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <ComboBox Grid.Column="1" Classes="filter" ItemsSource="{Binding StatusOptions}" ItemTemplate="{StaticResource ActivityFilterTemplate}" SelectionBoxItemTemplate="{StaticResource ActivityFilterTemplate}" SelectedItem="{Binding SelectedStatus}"/>
            <ComboBox Grid.Column="2" Classes="filter" ItemsSource="{Binding ProtocolOptions}" ItemTemplate="{StaticResource ActivityFilterTemplate}" SelectionBoxItemTemplate="{StaticResource ActivityFilterTemplate}" SelectedItem="{Binding SelectedProtocol}"/>
            <Button Grid.Column="3" Classes="outline" Content="{Binding PendingActivityLabel}" Command="{Binding ReturnToLatestCommand}" IsVisible="{Binding IsHistoryMode}"/>
          </Grid>
          <Grid Grid.Row="1" RowDefinitions="Auto,*,Auto">
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,1" Padding="12,9">
              <Grid ColumnDefinitions="100,1.15*,1.2*,90,76" ColumnSpacing="8"><TextBlock Text="{l:Locale activity.column.time}" Opacity="0.56" FontSize="10"/><TextBlock Grid.Column="1" Text="{l:Locale activity.column.model}" Opacity="0.56" FontSize="10"/><TextBlock Grid.Column="2" Text="{l:Locale activity.column.route}" Opacity="0.56" FontSize="10"/><TextBlock Grid.Column="3" Text="{l:Locale activity.column.status}" Opacity="0.56" FontSize="10"/><TextBlock Grid.Column="4" Text="{l:Locale activity.column.latency}" Opacity="0.56" FontSize="10"/></Grid>
            </Border>
            <ListBox x:Name="activityList" Classes="activity-list" Grid.Row="1" ItemsSource="{Binding Items}" Background="Transparent" BorderThickness="0" SelectionMode="Multiple" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate><VirtualizingStackPanel /></ItemsPanelTemplate>
              </ListBox.ItemsPanel>
              <ListBox.ItemTemplate><DataTemplate>
                  <Button Classes="row" HorizontalAlignment="Stretch" Command="{Binding $parent[UserControl].DataContext.SelectCommand}" CommandParameter="{Binding}">
                    <Border Padding="12,10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                      <Grid ColumnDefinitions="100,1.15*,1.2*,90,76" ColumnSpacing="8">
                        <StackPanel><TextBlock Text="{Binding Time}" FontFamily="Consolas" FontSize="11"/><TextBlock Text="{Binding RequestId}" Opacity="0.52" FontFamily="Consolas" FontSize="10"/></StackPanel>
                        <StackPanel Grid.Column="1"><TextBlock Text="{Binding ModelId}" FontWeight="SemiBold" FontSize="12" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding ProviderId}" Opacity="0.58" FontSize="10" TextTrimming="CharacterEllipsis"/></StackPanel>
                        <StackPanel Grid.Column="2"><TextBlock Text="{Binding Route}" FontFamily="Consolas" FontSize="10" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding Protocol}" Opacity="0.58" FontSize="10"/></StackPanel>
                        <TextBlock Grid.Column="3" Text="{Binding StatusLabel}" Foreground="{Binding StatusColor}" FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="{Binding Latency}" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center"/>
                      </Grid>
                    </Border>
                  </Button>
                </DataTemplate></ListBox.ItemTemplate>
            </ListBox>
            <Border Grid.Row="2" Background="{DynamicResource SurfaceSubtleBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="10,7" IsVisible="{Binding IsPullToRefreshVisible}">
              <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                <ProgressBar IsIndeterminate="{Binding IsLoadingMore}" Width="16" Height="16" IsVisible="{Binding IsLoadingMore}"/>
                <TextBlock Grid.Column="1" Text="{Binding LoadMoreLabel}" Opacity="0.66" FontSize="10" VerticalAlignment="Center"/>
              </Grid>
            </Border>
          </Grid>
        </Grid>
      </Border>
      <Border Grid.Column="1" Classes="panel" Padding="18">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel Spacing="13">
            <TextBlock Text="{l:Locale activity.detail.title}" Foreground="{DynamicResource AccentBrush}" FontSize="10" LetterSpacing="1.2"/>
            <TextBlock Text="{Binding SelectedModelLabel}" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding SelectedRequestIdLabel}" Opacity="0.62" FontSize="11"/>
            <Grid ColumnDefinitions="*,Auto" Margin="0,3,0,0"><TextBlock Text="{Binding SelectedItem.StatusCode}" FontSize="13" FontWeight="SemiBold"/><Button Grid.Column="1" Classes="outline" Content="{l:Locale activity.detail.copy.requestid}" Click="CopyRequestId_OnClick"/></Grid>
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="0,12,0,0"><Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto" RowSpacing="10" ColumnSpacing="10"><Border Grid.Row="0" Padding="9" Background="{DynamicResource SurfaceSubtleBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8"><StackPanel><TextBlock Text="{l:Locale activity.detail.field.protocol}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="10"/><TextBlock Text="{Binding SelectedItem.Protocol}" FontSize="11"/></StackPanel></Border><Border Grid.Row="0" Grid.Column="1" Padding="9" Background="{DynamicResource SurfaceSubtleBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8"><StackPanel><TextBlock Text="{l:Locale activity.detail.field.route}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="10"/><TextBlock Text="{Binding SelectedItem.Transform}" FontSize="11" TextWrapping="Wrap"/></StackPanel></Border><Border Grid.Row="1" Padding="9" Background="{DynamicResource SurfaceSubtleBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8"><StackPanel><TextBlock Text="{l:Locale activity.detail.field.upstream}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="10"/><TextBlock Text="{Binding SelectedItem.Latency}" FontSize="11"/></StackPanel></Border><Border Grid.Row="1" Grid.Column="1" Padding="9" Background="{DynamicResource SurfaceSubtleBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8"><StackPanel><TextBlock Text="{l:Locale activity.detail.field.size}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="10"/><TextBlock Text="{Binding SelectedItem.ResponseBytes}" FontSize="11"/></StackPanel></Border><Border Grid.Row="2" Grid.ColumnSpan="2" Padding="9" Background="{DynamicResource SurfaceSubtleBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8"><StackPanel><TextBlock Text="{l:Locale activity.detail.field.error}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="10"/><TextBlock Text="{Binding SelectedItem.ErrorType}" FontSize="11"/></StackPanel></Border></Grid></Border>
            <StackPanel Spacing="6"><TextBlock Text="{l:Locale activity.detail.summary.label}" FontWeight="SemiBold" FontSize="12"/><TextBlock Text="{Binding SelectedLogSummary}" FontFamily="Consolas" FontSize="10" TextWrapping="Wrap" Background="{DynamicResource SurfaceMutedBrush}" Padding="10"/></StackPanel>
            <TextBlock Text="{Binding Status}" Opacity="0.58" FontSize="10" TextWrapping="Wrap"/>
          </StackPanel>
        </ScrollViewer>
      </Border>
    </Grid>
  </Grid>
</UserControl>
