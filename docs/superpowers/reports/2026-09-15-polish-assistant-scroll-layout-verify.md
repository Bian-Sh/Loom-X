# 助手滚动布局验证

## 范围与结论
本次只修改 AssistantView 布局、局部滚动条主题及对应控件测试；不改助手业务、数据库、重试、异常气泡颜色。验证通过，等待用户视觉评审后确认归档。用户追加反馈“scrollbar 加粗”，已将可见胶囊从 4px 加粗到 6px、圆角从 2px 调整为 3px，拖动热区仍为 12px。

## 实现审查
- 新会话按钮删除 X=3 绘制变换，不再超出所属布局右侧；保留 22×22 点击范围。
- 独立滚动条右移 6px，保留 12px 热区，滑块实际宽 6px、圆角 3px、最小高度 28px；透明轨道，复用 TextTertiaryBrush；默认/悬停/按下不透明度 0.45/0.75/1。
- 原生 PART_Track 双向同步、PART_PageUpButton/PART_PageDownButton 保留，模板测试覆盖轨道点击与消息偏移同步，真实窗口验证拖动。
- 聊天区底部空隙清零，消息视口向下延伸 7px；输入卡 ZIndex=1，滚动条底端停在输入卡顶部。待批准时取消负间距，批准卡置于上层。
- 补充末条正文边界测试后发现普通状态行存在累计像素取整偏差，7px 补偿仍可能使正文底部进入输入卡 4px；改为末尾 14px 空白（补偿及少量呼吸空间），最终正文边界测试通过。重叠几何仍严格为 7px。

## 自动验证证据
- 红灯：原实现 5 项失败，21 项通过，失败对应旧按钮变换、旧间距及原滚动条模板。
- 最终控件测试：26 通过，0 失败，包括 420px/960px、单行/多行输入和批准状态。
- 最终 Debug 完整测试：656 通过，0 失败，耗时 34 秒。
- 最终 Release 完整测试：656 通过，0 失败，耗时 38 秒。
- 追加加粗调整后的控件测试：滚动条胶囊宽度 6px、圆角 3px 断言通过。
- 加粗调整后重新发布：outputs/20260915-183056/LoomX.exe。
- dotnet build LoomX.slnx --configuration Release --no-restore：成功，0 错误。
- git diff --check：通过；openspec validate polish-assistant-scroll-layout：通过。
- 现有 SQLitePCLRaw NU1903、空值和异步读取分析器警告未在本次处理。

## 视觉与交互证据
真实 Avalonia AssistantView 加模拟桌面着色背景，预览上下文没有桌面生命周期，不访问真实设置数据库。

- outputs/20260915-聊天滚动预览/着色背景.png，以及浅色/深色/灰色背景和滚动到底截图。追加效果见 outputs/20260915-聊天滚动预览-加粗/着色背景.png 与滚动到底.png。
- 布局实测：视口底部 Y=567、输入卡顶部 Y=560、滚动条底部 Y=560。
- CUA 只截图目标窗口，拖动后偏移从约 1266.65 变为 1725.42，截图中的消息与滑块均发生变化。驱动返回 effect=unverifiable，结合预览端偏移记录和前后截图确认实际效果，不单独把驱动返回当作成功证据。
- 临时预览已关闭，保留本次预览源码及图片，不清理其他任务文件。透明合成截图不用于断言真实系统桌面的最终配色。

## 发布与协作
- scripts/publish-desktop.ps1 发布成功：outputs/20260915-173022/LoomX.exe，自包含 win-x64 文件夹。未启动完整发布应用，避免修改真实用户会话与数据库。
- 按用户 AGENTS.md 的明确协作要求保留共享 master，只提交本次文件并推送，不创建或清理 worktree。
- 其他任务的 incremental-config-edit 轨迹修改、.zcode/、.local/ 中非本次文件原样保留。
- Comet 守卫无法推断 .NET 构建命令，在上述真实构建测试已通过后仅对守卫临时设置 COMET_SKIP_BUILD=1，不修改全局技能。
- 既有 geometric repack 的 bad tree object 警告仍存在，未进行仓库修复或清理。
