## 1. 回归测试

- [x] 1.1 增加标题按钮对齐、滚动条展开方向、输入框动态高度与键盘行为、模型弹层尺寸的失败测试，并确认测试因现有实现缺少目标行为而失败。
- [x] 1.2 增加真实路由回归测试，确认普通 Enter 在多行 `TextBox` 默认处理前未被拦截时测试失败。

## 2. 界面实现

- [x] 2.1 调整 `AssistantView` 的标题按钮、消息滚动条、自动增高输入框和模型选择弹层，并确认定向测试全部通过。
- [x] 2.2 根据实机反馈进一步压缩模型弹层尺寸与列表行高，并确认密度契约测试通过。
- [x] 2.3 根据第二轮反馈将模型弹层宽度再缩小 40px 至 256px，并重新完成回归与实机验证。
- [x] 2.4 让模型搜索框透出透明主题，并为越界模型名增加省略号和完整名称悬停气泡。
- [x] 2.5 修复模型搜索框聚焦态的 Fluent 模板白底，并用真实聚焦控件验证模板背景透明。
- [x] 2.6 根据实机反馈修复 Ctrl+Enter 换行、外置消息滚动条、失败状态横向漂移和消息区底部 2px 间距。
- [x] 2.7 将 Enter 发送处理移至父级隧道路由，避免多行输入框先消费 Enter，并保持 Shift+Enter、Ctrl+Enter 的默认换行。

## 3. 验证与发布

- [x] 3.1 执行完整测试、Release 构建和真实桌面交互验证，并将新的 `win-x64` 发布包输出到带可读时间的 `outputs/` 目录。
- [x] 3.2 重新执行定向测试、完整测试、Release 构建和桌面实机验证，并生成新的时间命名发布包。
- [x] 3.3 对 Enter 路由修复重新执行 21 项助手视图测试、626 项完整测试、Release 构建、发布和桌面实机按键验证。
