# AppDataStore 数据中心

## 目标

为桌面进程引入长期存活的 `AppDataStore`，集中管理配置快照、页面 ViewModel 生命周期和活动滑动窗口，减少导航和刷新造成的 SQLite 重复读取。

## 范围

- 启动后异步预加载配置并发布变更事件。
- 复用概览、Provider、网关、活动、控制台和设置 ViewModel。
- 配置写入成功后刷新内存快照，失败时保留原快照。
- 活动按 `(CreatedAt, Id)` 游标分页，维护最多 500 条窗口并支持历史/最新模式。
- 保持控制台日志缓冲和滚动行为不变。
