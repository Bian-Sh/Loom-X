## 1. UI 契约测试

- [x] 1.1 增加右侧模型组合面板隐藏不存在 Combo、左侧 flags 下拉框仍保留不存在 Combo 的契约断言，并运行定向测试确认新增断言先失败

## 2. 最小实现

- [x] 2.1 在右侧 Combo 项根容器绑定 `IsVisible` 到 `!IsDeleted`，保持左侧模板不变，并运行定向测试确认通过

## 3. 验证

- [x] 3.1 运行网关视图相关测试与项目构建，确认无回归且 OpenSpec 校验通过