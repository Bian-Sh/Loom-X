## 1. 自动重试策略

- [x] 1.1 先补充失败测试，覆盖可恢复错误总共尝试 5 次、确定性错误只尝试一次、已有流式内容后不重试以及取消等待，并确认测试按预期失败。
- [x] 1.2 实现结构化 `Retry-After` 与 AgentLoop 步骤级有限重试，补齐安全的开始、重试、耗尽和短路日志，并确认 AgentLoop 与模型客户端定向测试通过。

## 2. 终态异常气泡

- [x] 2.1 先补充失败测试，要求 `TaskFailed` 投影为专属 Error 消息、移除手动命令和按钮，并约束异常气泡字号、字重、颜色 token 与阅读表面。
- [x] 2.2 实现消息流异常类型、历史重放和淡红透明气泡，删除独立错误卡片及手动操作，并确认 ViewModel 与 Avalonia 视觉契约测试通过。

## 3. 透明度与交付验证

- [x] 3.1 为异常阅读表面增加 Alpha 下限测试并实现外观协调逻辑，确认全局透明度最低值下仍保持可读背景。
- [x] 3.2 运行 OpenSpec 验证、完整测试和 Release 构建，启动桌面端检查异常气泡实际效果，并生成 `outputs/` 时间命名发布包供预览。
