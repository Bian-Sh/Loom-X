# 修复单实例重复启动无反馈

## 问题

LoomX 已经运行时再次启动，第二个进程检测到 `Local\\LoomX` 互斥锁后直接退出。已有窗口不会恢复或前置，用户只能看到启动无反应。

## 根因

单实例检查只有互斥锁和日志，没有跨进程激活协议；主窗口也没有统一的恢复、激活和 Windows 前置入口。

## 目标

1. 第二次启动向首实例发送固定的激活请求后退出。
2. 首实例恢复最小化窗口并将其激活到前台。
3. 通信失败时保持原有单实例安全边界，快速退出并记录安全摘要。
4. 覆盖协议、失败降级和现有调试多实例开关的回归测试。
