## 1. 回归测试

- [x] 1.1 增加历史会话必须使用平台 Popup 宿主的失败测试，并确认旧的独立 Window 实现使测试失败。

## 2. 根因修复

- [x] 2.1 将历史浮层迁回 Avalonia 原生 Popup，移除独立 Window 与 owner 激活补偿，并确认定向测试通过。

## 3. 验证与发布

- [x] 3.1 执行完整测试、Release 构建、真实 Win32 前台序列与无闪烁交互验证，并重新发布 `win-x64` 到带可读时间的 `outputs/`。
