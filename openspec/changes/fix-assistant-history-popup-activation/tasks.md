## 1. 回归测试

- [x] 1.1 增加主动关闭浮窗后恢复 owner 激活状态的失败测试，并确认测试因缺少该行为而失败。

## 2. 最小修复

- [x] 2.1 为锚定浮窗增加关闭后恢复 owner 的入口，历史会话 cell 使用该入口，并确认定向测试通过。

## 3. 验证与发布

- [x] 3.1 执行完整测试、Release 构建、真实 Win32 前台交互验证，并重新发布 `win-x64` 到带可读时间的 `outputs/`。
