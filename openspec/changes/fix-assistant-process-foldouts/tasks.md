# 任务

## 1. 回归测试

- [x] 1.1 增加多 step、多工具仍只有一个父过程组且最终内容完成前保持“处理中”的失败测试，并验证测试按预期失败。

## 2. 最小修复

- [x] 2.1 修正过程组生命周期、历史重放和父子 foldout 状态，更新中文资源与 XAML，并验证定向 ViewModel 测试通过。

## 3. 集成验证

- [x] 3.1 执行相关测试、完整测试与构建，重新发布 `win-x64` 到带可读时间的 `outputs/`，启动发布包核对进程路径和实际界面。

<!-- review skipped: hotfix 预设 review_mode=off；已执行定向测试、完整回归、Release 构建与发布包 GUI 验收。 -->
