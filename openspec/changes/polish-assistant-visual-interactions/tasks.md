> **2026-09-20 对账记录：** 根据当前实现、自动化测试、Release 构建、时间命名发布包及 CUA 实机验证，补齐此前未同步的完成状态。

## 1. 回归测试

- [x] 1.1 为 Markdown 彩色图标依赖、贴边滚动轨、异常透明材质和新会话图标偏移补充失败测试，并确认失败原因与四项需求对应。

## 2. 实施

- [x] 2.1 固定支持彩色 emoji 的 SkiaSharp 与 Win32 原生资产版本，并验证项目恢复成功。
- [x] 2.2 实现顶部/底部三角按钮、主题胶囊滑块和窗口右侧贴边布局，并验证滚动同步与端点点击。
- [x] 2.3 将异常气泡切换到普通透明主题链路，并将新会话图标在上一轮反馈基线上继续右移 8px，验证历史图标位置不变且按钮未被裁剪。

## 3. 验证与交付

- [x] 3.1 运行助手视图测试、完整测试和 Release 构建，确认无回归。
- [x] 3.2 在真实应用中验证历史 Markdown 图标与滚动条视觉，重新发布到 `outputs` 的可读时间目录并记录结果。
