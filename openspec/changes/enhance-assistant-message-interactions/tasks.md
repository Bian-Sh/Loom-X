## 1. 文字选择

- [x] 1.1 先补充用户消息和异常消息正文必须使用可选择文本控件的视图契约测试，并运行相关测试确认因当前 `TextBlock` 实现失败
- [x] 1.2 将用户消息和异常消息正文改为 `SelectableTextBlock`，运行相关测试确认选择能力契约通过且原有样式断言保持通过

## 2. 中键自动滚动

- [x] 2.1 先补充自动滚动速度、死区、方向和边界计算测试，并运行测试确认缺少目标实现时失败
- [x] 2.2 在聊天消息区增加自动滚动锚点、指针事件、计时滚动、方向反馈及完整退出清理，运行相关测试确认通过
- [x] 2.3 稳定底部模型选择器的常态表面和垂直对齐，运行视图测试并对比划入前后截图确认不再产生悬浮错觉

## 3. 集成验证与交付

- [x] 3.1 运行助手视图相关测试和完整测试集，确认现有滚动条同步、跟随最新消息及气泡布局没有回归
- [x] 3.2 发布 Windows 桌面包到 `outputs` 下带可读时间的目录，并人工验证中键启动、方向滚动、退出和文字划选

## 4. 混合文字基线修正

- [x] 4.1 补充助手正文文本字体优先、中文与彩色 Emoji 回退顺序的视图契约测试，并确认旧字体链测试失败
- [x] 4.2 调整助手页与 Markdown 正文字体链，保持英文、数字、中文基线一致并保留彩色 Emoji 回退
- [x] 4.3 运行完整测试，重新发布桌面包，并对比英文数字选择前后与 Emoji 渲染截图
- [x] 4.4 补充 Markdown 普通文字与 Emoji 必须拆分为独立字体运行段的回归测试，并确认旧实现因单一隐式回退链失败
- [x] 4.5 在 LiveMarkdown 节点管线中为普通文字与 Emoji 生成显式字体运行段，并保持流式更新和文字选择
- [x] 4.6 运行完整测试、重新发布桌面包，并验证网关页面往返后 Emoji 仍彩色且混合文字基线稳定
